IMPLEMENTATION TASK LIST - Dynamic SDK Branch Testing
======================================================

COMPLETED ✅
-----------
1. ✅ requirements-pr.txt template with {{BRANCH_NAME}} placeholders
2. ✅ Dockerfile updated with ARG and force-reinstall logic
3. ✅ Workflow branch detection and sed substitution


TO DO 📋
--------
4. [ ] Test sed substitution locally with branch names containing slashes

5. [ ] Test Docker build locally
    - Generate requirements-pr-generated.txt manually
    - Build and verify SDK packages are from PR branch

6. [ ] Create .github/workflows/monitor-sdk-prs.yml
    - Cron schedule: */15 * * * *
    - Poll microsoft/Agent365-python for open PRs
    - Trigger Docker builds for each PR branch

7. [ ] Add PR tracking to avoid re-testing unchanged PRs
    - Use GitHub cache or artifacts to store last tested commit SHA

8. [ ] Test polling workflow with workflow_dispatch

9. [ ] Add requirements-pr-generated.txt to .gitignore

10. [ ] Update README.md with setup documentation

11. [ ] End-to-end test with actual SDK PR


QUICK COMMANDS
--------------
# Test sed (PowerShell)
$env:BRANCH_NAME="copilot/implement-chat-history-api"
Get-Content requirements-pr.txt | ForEach-Object { $_ -replace '{{BRANCH_NAME}}', $env:BRANCH_NAME }

# Build Docker
docker build -t agent-framework-test:local .

# Test imports
docker run --rm agent-framework-test:local python -c "import microsoft_agents_a365_tooling; print('OK')"
